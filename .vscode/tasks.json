{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "npm",
            "script": "debug.buildrs.watch",
            "problemMatcher": [],
            "label": "yarn: debug.buildrs.watch",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
